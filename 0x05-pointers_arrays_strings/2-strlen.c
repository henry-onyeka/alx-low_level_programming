#include "main.h"
/**
 * _strlen - this functions iterates and counts the strings in the array
 * @s: thats the string arrays
 * Return: returns thelenght of the array
 */
int _strlen(char *s)
{
	int p;

	for (p = 0; s[p] != '\0'; ++p)

	{
	}

	return (p);
}

