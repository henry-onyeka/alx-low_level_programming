#include <stdio.h>
/**
 * main - make i count one two three like nursery school pikin
 * Return: Always 0 (success)
 */
int main(void)
{
	int m;

	for (m = 38 ; m < 48 ; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
