#include <stdio.h>
/**
 * main - make i count one two three like nursery school pikin
 * Return: Always 0 (success)
 */
int main(void)
{
	int m;

	for (m = 0 ; m < 10 ; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
