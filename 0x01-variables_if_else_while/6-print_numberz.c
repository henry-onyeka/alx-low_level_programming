#include <stdio.h>
/**
 * main - make i count one two three like nursery school pikin
 * Return: Always 0 (success)
 */
int main(void)
{
	int num;

	for (m = 0 ; m <= 9 ; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
