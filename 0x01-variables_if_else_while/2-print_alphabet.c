#include <stdio.h>
/**
* main - me just wan do magic here i no send anybody
* Return: always 0 (success)
*/
int main(void)
{
	char henry = 'a';
	while (henry <= 'z')
{
	putchar(henry);
	henry++;
}
	putchar(0);
	return (0);
}
