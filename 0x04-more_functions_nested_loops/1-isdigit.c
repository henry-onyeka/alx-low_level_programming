#include "main.h"
/**
 * _isdigit - checks if its a digit
 * @c: character to checked for digit membership
 * Return: always 1 for this project
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')

	return (1);

return (0);
}
