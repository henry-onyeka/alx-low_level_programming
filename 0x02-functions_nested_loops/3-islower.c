#include "main.h"
/**
 * _islower - just an if statement
 * @c: is an Ascii character
 * Return: always 0 bros
*/
int _islower(int c)

{
	if (c >= 'a' && c <= 'z')

{
		return (1);

}
	else
{
		return (0);
}
}
