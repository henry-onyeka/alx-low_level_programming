#include <stdio.h>
#include "main.h"
/**
 * main - the main function
 * @argv: the firs argument will be passed unto
 * @argc: the number it will return
 * Return: returns nothing
 */
int main(int argc, char *argv[])
{

	printf("%s\n", argv[0]);
	return (0);
}
